<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.firebaseauthexample">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo_app_foreground"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirebaseAuthExample">
        <activity
            android:name="screen.learninfo.LearnInfoActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name="screen.test.ui.TestActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name="screen.registration.ui.RegistrationActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name="screen.quiz.QuizActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name="screen.login.ui.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>